node_modules/
.wrangler/
.dev.vars
.env
dist/
*.log

# Local machine paths and personal permission grants
.claude/settings.local.json

# Files carrying the worker URL or Cloudflare account ID — kept local only.
# README.md and HANDOFF.md were sanitised to $WORKER_URL / $CF_ACCOUNT_ID
# placeholders and are now tracked; scan for the literal .env values before
# committing either of them again. The repo is PUBLIC and the Worker's write
# endpoints are unauthenticated, so the URL itself is the thing to withhold.
AI_Research.md
benchmark/BENCHMARK.md
liverun.html

# Session scratch: run dumps, probe output, the bug 24 test harness
.runs/
