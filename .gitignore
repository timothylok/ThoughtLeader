node_modules/
.wrangler/
.dev.vars
.env
dist/
*.log

# Local machine paths and personal permission grants
.claude/settings.local.json

# Files carrying the worker URL or Cloudflare account ID — kept local only.
# README.md, HANDOFF.md and AI_Research.md were sanitised to $WORKER_URL /
# $CF_ACCOUNT_ID placeholders and are now tracked; scan for the literal .env
# values before committing any of them again. The repo is PUBLIC.
#
# The write endpoints are no longer unauthenticated — CONTROL_TOKEN gates every
# route that writes or spends (bugs.md #33) — so the URL is defence in depth
# now rather than the only defence. Withhold it anyway.
benchmark/BENCHMARK.md
liverun.html

# Session scratch: run dumps, probe output, the bug 24 test harness
.runs/
